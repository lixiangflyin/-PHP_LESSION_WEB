<?php
/**
兼容Perl的Regex

1.定界符号 //

2.原子 : 正则表达式的最基本组成单位,而且必须至少要包含一个原子
	   (只有一个正则表达式可以单独使用的字符,就是原子)

 */


$pattern = '//';

$string = '';


$match_time = preg_match($pattern, $subject);



if($match_time == false || $match_time == 0)
{
	echo 'no match';
}
else
{
	echo 'match time:' . $match_time;
}
?>









